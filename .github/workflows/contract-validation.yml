name: Contract Validation

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-contracts:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate WhyCode contracts
        run: ./scripts/validate-whycode-contracts.sh
